import { pool } from '../src/db/pool.js';

if (!process.env.DATABASE_URL) throw new Error('DATABASE_URL is required');
const examples = [
  ['Образование', 'Подбор учебных материалов', 'Преподаватели вручную ищут материалы для каждой темы.', 'Подбирать упражнения под тему и уровень группы.', 'Преподаватели учебного центра'],
  ['Образование', 'Расписание консультаций', 'Студенты согласуют встречи с наставниками в нескольких чатах.', 'Собрать свободные интервалы наставников в одном расписании.', 'Студенты и наставники'],
  ['Образование', 'Обратная связь по занятиям', 'Анкеты после занятий хранятся в отдельных таблицах.', 'Показывать повторяющиеся замечания и динамику оценок.', 'Методисты курсов'],
  ['Образование', 'Карта навыков выпускников', 'Результаты практических работ трудно сравнить между группами.', 'Составлять профиль освоенных навыков по выполненным заданиям.', 'Кураторы и выпускники'],
  ['Торговля', 'Контроль остатков магазина', 'Сотрудники проверяют наличие товаров по нескольким таблицам.', 'Показывать товары, которые скоро закончатся.', 'Менеджеры небольшого магазина'],
  ['Торговля', 'Анализ отзывов покупателей', 'Отзывы поступают с разных площадок и читаются вручную.', 'Группировать отзывы по темам и частым проблемам.', 'Команда клиентского сервиса'],
  ['Торговля', 'Планирование акций', 'Менеджер сравнивает результаты акций в отдельных отчётах.', 'Сопоставлять продажи до и после акции на наглядной панели.', 'Менеджеры по продажам'],
  ['Торговля', 'Каталог локальных производителей', 'Информация о поставщиках собрана в переписке.', 'Создать каталог с поиском по категории и условиям поставки.', 'Закупщики'],
  ['Логистика', 'Отслеживание статусов доставки', 'Операторы узнают статус заказа по телефону.', 'Показывать текущий этап доставки в едином списке.', 'Операторы службы доставки'],
  ['Логистика', 'Планирование загрузки склада', 'Приёмка грузов назначается без общего расписания.', 'Распределять интервалы приёмки и показывать занятость.', 'Диспетчеры склада'],
  ['Логистика', 'Учёт возвратной тары', 'Перемещения ящиков фиксируются в бумажном журнале.', 'Вести историю выдачи и возврата тары.', 'Кладовщики'],
  ['Логистика', 'Анализ задержек поставок', 'Причины опозданий записываются свободным текстом.', 'Группировать причины задержек и показывать их частоту.', 'Координаторы поставок'],
  ['Туризм', 'Конструктор городских маршрутов', 'Гиды составляют похожие прогулки вручную.', 'Собирать маршрут по интересам и доступному времени.', 'Гиды и гости города'],
  ['Туризм', 'Бронирование экскурсий', 'Записи на экскурсии ведутся в мессенджере.', 'Показывать свободные места и собирать заявки.', 'Администраторы экскурсионного бюро'],
  ['Туризм', 'Путеводитель по локальным событиям', 'Анонсы мероприятий разбросаны по разным страницам.', 'Объединять события в календарь с тематическими фильтрами.', 'Туристы и жители города'],
  ['Туризм', 'Сбор обратной связи гостей', 'Отзывы о поездках сложно связать с конкретными программами.', 'Показывать оценки и предложения по каждой программе.', 'Организаторы туров'],
  ['Услуги', 'Запись в мастерскую', 'Мастер принимает заявки по телефону и теряет детали.', 'Создать форму заявки и расписание приёма.', 'Мастера ремонтной мастерской'],
  ['Услуги', 'Планирование выездных работ', 'Задания сотрудникам распределяются в чатах.', 'Показывать занятость специалистов и адреса выездов.', 'Координаторы сервисной компании'],
  ['Услуги', 'Учёт оборудования в аренде', 'Статусы оборудования обновляются в общей таблице.', 'Отображать доступность, выдачу и срок возврата.', 'Менеджеры пункта проката'],
  ['Услуги', 'База ответов клиентам', 'Сотрудники каждый раз заново пишут ответы на типовые вопросы.', 'Собирать проверенные ответы с поиском по теме.', 'Операторы поддержки'],
  ['Экология', 'Карта пунктов приёма вторсырья', 'Жители не знают, какие материалы принимают ближайшие пункты.', 'Показывать пункты приёма с фильтром по материалам.', 'Жители города'],
  ['Экология', 'Учёт расхода бумаги', 'Расход материалов фиксируется только при закупке.', 'Показывать потребление бумаги по подразделениям.', 'Офис-менеджеры'],
  ['Культура', 'Регистрация волонтёров фестиваля', 'Заявки волонтёров приходят через несколько каналов.', 'Объединить заявки и предпочтения по сменам.', 'Организаторы фестиваля'],
  ['Культура', 'Каталог экспонатов музея', 'Описания экспонатов хранятся в разрозненных документах.', 'Создать поиск по коллекции и карточки экспонатов.', 'Сотрудники небольшого музея'],
];
const fixed_id = (group, index) => `00000000-0000-4000-8000-${group}${String(index).padStart(10, '0')}`;
const client = await pool.connect();
try {
  await client.query('BEGIN');
  await client.query('SELECT pg_advisory_xact_lock(696369)');
  const owner = fixed_id('01', 1);
  await client.query("INSERT INTO users(id,name,role) VALUES ($1,'Демо бизнес 1','business') ON CONFLICT DO NOTHING", [owner]);
  let added = 0;
  for (const [index, [industry, title, context, need, audience]] of examples.entries()) {
    const task_id = fixed_id('07', index + 1);
    // Six completeness levels: 20, 40, 55, 70, 90 and 100 points.
    const level = index % 6;
    const card = {
      title, industry, context, need,
      data_materials: level >= 1 ? 'Синтетический набор из 100 записей и примеры текущих форм. Реальные персональные данные не используются.' : '',
      expected_result: level >= 2 ? `Рабочий веб-прототип: ${need}` : '',
      success_criteria: level >= 3 ? 'Пять тестовых пользователей выполняют основной сценарий без помощи; поиск записи занимает не более 10 секунд.' : '',
      constraints_description: level >= 4 ? 'Срок — две недели. Использовать только синтетические данные и запуск в браузере.' : '',
      users_description: level >= 4 ? audience : '',
      contact: level >= 5 ? `demo-${index + 1}@example.com` : '',
      interaction_format: level >= 5 ? 'Онлайн-консультация с представителем бизнеса раз в неделю.' : '',
      feedback_process: level >= 5 ? 'Демонстрация по пятницам; комментарии к результату в течение двух рабочих дней.' : '',
    };
    const fields = Object.keys(card);
    const inserted = await client.query(`INSERT INTO tasks(id,owner_id,original_description,${fields.join(',')})
      VALUES ($1,$2,$3,${fields.map((_, i) => `$${i + 4}`).join(',')}) ON CONFLICT DO NOTHING RETURNING id`,
      [task_id, owner, `[Синтетическая задача] ${context} ${need}`, ...Object.values(card)]);
    // Skip existing fixtures entirely, preserving later edits and publication decisions.
    if (!inserted.rowCount) continue;
    const revision_id = fixed_id('08', index + 1);
    await client.query('INSERT INTO task_revisions(id,task_id,confirmed_by,card) VALUES ($1,$2,$3,$4)', [revision_id,task_id,owner,card]);
    await client.query('UPDATE tasks SET published_revision_id=$2,published_at=now() WHERE id=$1', [task_id,revision_id]);
    added++;
  }
  await client.query('COMMIT');
  console.log(`Added ${added} published mock tasks; fixture contains ${examples.length} tasks.`);
} catch (error) {
  await client.query('ROLLBACK');
  console.error('Catalog seed failed:', error.message);
  process.exitCode = 1;
} finally {
  client.release();
  await pool.end();
}
